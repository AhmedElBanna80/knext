name: Compat suite (official Next.js deploy harness)

# ─────────────────────────────────────────────────────────────────────────────
# #89 / ADR-0007 A3-2 — the AUTHORITATIVE (slow) compatibility gate.
#
# This runs the OFFICIAL Next.js adapter compatibility harness: it checks out
# vercel/next.js at a PINNED ref, builds it, and runs `run-tests.js` in
# NEXT_TEST_MODE=deploy against knext's lifecycle scripts (scripts/e2e-*.sh) +
# the deploy manifest (test/deploy-tests-manifest.knext.json).
#
# HONESTY: this is an MVP SCAFFOLD. It runs a REAL but PARTIAL subset (modest
# shard count, Node runtime only to start). It does NOT yet pass the full suite,
# and its mere existence must NOT be read as "knext passes the official suite".
# The compat-matrix official-suite row stays ❌ until a green nightly (a separate
# graduation PR, ADR-0007 A3-3). The exclude-list (the manifest) is an honest
# ledger that grows from OBSERVED failures, never a pre-emptive fake green.
#
# Deliberately NOT in ci.yml: heavyweight (clones+builds Next.js + Playwright).
# Nightly + manual dispatch only — the fast per-PR gate is the compat-smoke job.
# ─────────────────────────────────────────────────────────────────────────────

on:
  workflow_dispatch:
    inputs:
      nextjsRef:
        description: 'vercel/next.js git ref to test against (pinned tag; do NOT use canary)'
        required: false
        default: 'v16.0.3'
  schedule:
    # 03:17 UTC nightly — off-peak; the run takes the better part of an hour.
    - cron: '17 3 * * *'

# Pin the Next.js ref so a failure is attributable to knext, not upstream churn.
# Aligns with the in-repo next@16.0.3 (CLAUDE.md / ADR-0007 version-skew caveat).
env:
  NEXTJS_REF: ${{ github.event.inputs.nextjsRef || 'v16.0.3' }}

jobs:
  build-next:
    name: Build vercel/next.js (pinned)
    runs-on: ubuntu-latest
    # #147 step 1 (round 2) — round 1 raised this 60→120 and scoped nothing, so
    # the COLD full-monorepo build (the unscoped `turbo run build`
    # over docs/examples/eslint-plugin-next/create-next-app/turbopack JS bench/…)
    # hit the 120-min ceiling EXACTLY (run 28285404942: 09:37:29Z→11:37:46Z) and
    # was cancelled, so the deploy-tests shards never ran. The reference harness
    # affords 180 min AND leans on a Turbo REMOTE cache (vercel-only) — we have
    # neither. Compounding: actions/cache only SAVES on job SUCCESS, so a build
    # that never completes never populates the cache → every run is a cold miss.
    #
    # Fix: build only what the deploy tests need — they invoke `next build`
    # against fixture apps (scripts/e2e-deploy.sh), which requires the `next`
    # package and its workspace dependency closure, NOT the whole monorepo. We
    # scope the build with `turbo run build --filter=next...` (trailing `...`
    # pulls in next's workspace deps: @next/env, @next/font, the polyfills,
    # @next/react-refresh-utils). Plus we raise the timeout to the reference's
    # 180-min ceiling as headroom for the first cold build to COMPLETE and
    # finally populate the #148 build cache.
    #
    # Note we do NOT compile @next/swc from Rust source: turbo's `build` task is
    # JS (`dependsOn: ["^build"]`, no build-native), and the native binary comes
    # from the prebuilt npm postinstall during install — the same path the
    # reference takes for deploy tests (skipNativeBuild: yes).
    timeout-minutes: 180
    steps:
      - name: Checkout knext
        uses: actions/checkout@v4
        with:
          path: knext

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        # Pin explicitly: knext is checked out under `path: knext`, so the action
        # cannot resolve a `packageManager` field at the workspace root and would
        # die with "No pnpm version is specified" — which skipped the deploy-tests
        # shards entirely. Keep in sync with `packageManager` in package.json.
        with:
          version: 10.4.1

      - name: Install knext deps
        working-directory: knext
        run: pnpm install --frozen-lockfile

      # Build the runtime + the adapter package so `npm pack @knext/core` ships a
      # working adapter for NEXT_ADAPTER_PATH.
      - name: Build @knext/lib + @knext/core
        working-directory: knext
        run: |
          pnpm --filter @knext/lib build
          pnpm --filter @knext/core build

      - name: Pack @knext/core adapter tarball
        working-directory: knext/packages/kn-next
        run: |
          npm pack
          ls -1 *.tgz

      - name: Checkout vercel/next.js (pinned ref)
        uses: actions/checkout@v4
        with:
          repository: vercel/next.js
          ref: ${{ env.NEXTJS_REF }}
          path: next.js

      # IMPORTANT (the #137 follow-up): next.js v16.0.3 declares its OWN required
      # pnpm (`packageManager: pnpm@9.6.0`), which CLASHES with the knext pin
      # (10.4.1) set above via pnpm/action-setup. A single global pnpm cannot
      # satisfy both — running the next.js install under 10.4.1 fails the engine
      # check ("Unsupported engine ... wanted pnpm 9.6.0") and SKIPs every shard.
      # Fix: drive the next.js steps through corepack, which auto-selects pnpm
      # per-project from next.js's `packageManager` field. We call `corepack
      # pnpm` explicitly (not bare `pnpm`) so it resolves next.js's 9.6.0 rather
      # than the action-setup 10.4.1 shim on PATH. The knext build steps above
      # stay on 10.4.1; never hardcode 9.6.0 here — let corepack honor upstream.
      - name: Enable corepack (next.js per-project package manager)
        working-directory: next.js
        run: corepack enable

      # #147 step 1 — resolve next.js's package store dir so we can persist it
      # across runs. The reference harness leans on a Turbo REMOTE cache
      # (vercel-only secret); we have no remote cache, so actions/cache is the
      # substitute.
      - name: Resolve package store path
        id: pnpm-store
        working-directory: next.js
        run: echo "path=$(corepack pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      # Cache the pnpm content-addressable store keyed on NEXTJS_REF + the next.js
      # lockfile, so a REPEAT run skips re-downloading every dependency (incl. the
      # prebuilt @next/swc native binary). NEXTJS_REF is in the key so a ref bump
      # invalidates it — the store for v16.0.3 is not valid for a different ref.
      - name: Cache next.js pnpm store
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: nextjs-pnpm-store-${{ env.NEXTJS_REF }}-${{ hashFiles('next.js/pnpm-lock.yaml') }}
          restore-keys: |
            nextjs-pnpm-store-${{ env.NEXTJS_REF }}-

      # #147 — GHA-backed Turbo REMOTE cache (the real fix for the cold-build
      # chicken-and-egg). The prior #148 build-output `actions/cache` only SAVED
      # on job SUCCESS, but the cold next.js build was cancelled at the timeout
      # every run, so that cache never populated → every run was a fresh ~3h cold
      # miss. We REPLACE it with a turbo remote cache: turbo uploads each next.js
      # package's build output INCREMENTALLY the moment its `build` task finishes,
      # so a cancelled run still PERSISTS the tasks that completed, the next run
      # RESUMES from them, and across a few dispatches the cache fills until one
      # build completes — exactly the property `actions/cache` could not give us.
      #
      # Turbo 2.5.5 (next.js's root devDep) has NO native GitHub-Actions cache
      # backend — it only speaks the Vercel/custom remote-cache HTTP API via
      # TURBO_API/TURBO_TOKEN/TURBO_TEAM (the reference harness uses Vercel's,
      # which is not available to us). This community action runs a tiny local
      # server that proxies that HTTP API to GitHub Actions' built-in cache and
      # `core.exportVariable`s TURBO_API/TURBO_TOKEN/TURBO_TEAM into $GITHUB_ENV,
      # so the later `corepack pnpm turbo run build` step inherits them and uses
      # the remote cache automatically (no --no-cache / no remoteCache disable).
      #
      # SUPPLY CHAIN (security.md): pinned by full commit SHA, tag in comment.
      # NOTE: the GitHub Actions cache is capped at 10 GB/repo with LRU eviction.
      # If next.js's build artifacts exceed that, eviction may prevent the cache
      # fully warming across runs; the larger-runner alternative is tracked on
      # #147 (graduation checklist item 1).
      - name: Set up GHA-backed Turbo remote cache
        # dtinth/setup-github-actions-caching-for-turbo v1.3.0
        uses: dtinth/setup-github-actions-caching-for-turbo@cc723b4600e40a6b8815b65701d8614b91e2669e
        with:
          cache-prefix: nextjs-turbo-${{ env.NEXTJS_REF }}-

      - name: Setup Next.js build deps + Playwright
        working-directory: next.js
        run: |
          corepack pnpm install
          corepack pnpm playwright install chromium --with-deps

      # #147 step 1 (round 2) — TARGETED build. The deploy tests only need the
      # `next` package + its workspace dependency closure (they run `next build`
      # against fixture apps). `--filter=next...` (trailing `...` = include
      # next's workspace deps) skips docs, examples, eslint-plugin-next,
      # create-next-app and the turbopack JS bench packages — the bulk of the
      # full-monorepo cold build that hit the 120-min timeout. The native
      # @next/swc binary still arrives via the prebuilt npm postinstall during
      # install above (we never compile it from Rust here). This step inherits
      # TURBO_API/TURBO_TOKEN/TURBO_TEAM from the remote-cache step above, so
      # turbo reads/writes the GHA-backed remote cache per task — no --no-cache.
      - name: Build Next.js (scoped to `next` + workspace deps)
        working-directory: next.js
        run: corepack pnpm turbo run build --filter=next...

      # Cache the prepared knext + next.js trees for the shard jobs.
      - name: Upload workspace
        uses: actions/upload-artifact@v4
        with:
          name: compat-workspace
          path: |
            knext
            next.js
          retention-days: 1
          include-hidden-files: true

  deploy-tests:
    name: Deploy tests (shard ${{ matrix.shard }})
    needs: build-next
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        # MVP: a MODEST 4-way shard (the reference uses 16). Bump deliberately as
        # the suite stabilizes; 4 keeps the nightly cost honest for the scaffold.
        shard: ['1/4', '2/4', '3/4', '4/4']
    steps:
      - name: Download workspace
        uses: actions/download-artifact@v4
        with:
          name: compat-workspace

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        # Same pin as build-next: the workspace is the downloaded artifact, with
        # no resolvable `packageManager` at its root. Keep in sync with package.json.
        with:
          version: 10.4.1

      - name: Make lifecycle scripts executable
        working-directory: knext
        run: chmod +x scripts/e2e-deploy.sh scripts/e2e-logs.sh scripts/e2e-cleanup.sh

      - name: Run official deploy tests (knext adapter)
        working-directory: next.js
        env:
          NEXT_TEST_MODE: deploy
          # knext's lifecycle scripts (the deploy-script contract).
          NEXT_TEST_DEPLOY_SCRIPT_PATH: ${{ github.workspace }}/knext/scripts/e2e-deploy.sh
          NEXT_TEST_DEPLOY_LOGS_SCRIPT_PATH: ${{ github.workspace }}/knext/scripts/e2e-logs.sh
          NEXT_TEST_CLEANUP_SCRIPT_PATH: ${{ github.workspace }}/knext/scripts/e2e-cleanup.sh
          # The package dir e2e-deploy.sh `npm pack`s + installs into each temp app.
          ADAPTER_DIR: ${{ github.workspace }}/knext/packages/kn-next
          # Layer knext's exclude/include ledger on top of Next's deploy-eligible set.
          NEXT_EXTERNAL_TESTS_FILTERS: ${{ github.workspace }}/knext/test/deploy-tests-manifest.knext.json
          KNEXT_RUNTIME: node
        # `|| true`: a partial-subset scaffold WILL have failures today; we still
        # want the summary artifact emitted. The matrix row stays ❌ regardless.
        run: |
          node run-tests.js --type e2e -g ${{ matrix.shard }} 2>&1 | tee runner.log || true

      - name: Compute excluded count from manifest
        id: excl
        working-directory: knext
        run: |
          COUNT=$(node -e "console.log(require('./test/deploy-tests-manifest.knext.json').rules.exclude.length)")
          echo "count=${COUNT}" >> "$GITHUB_OUTPUT"

      - name: Summarize shard result
        working-directory: knext
        run: |
          SAFE_SHARD="${{ matrix.shard }}"
          SAFE_SHARD="${SAFE_SHARD/\//-}"
          node scripts/e2e-summary.mjs \
            --runner-log "${{ github.workspace }}/next.js/runner.log" \
            --ref "${NEXTJS_REF}" \
            --shard "${{ matrix.shard }}" \
            --excluded "${{ steps.excl.outputs.count }}" \
            --out "compat-suite-summary-${SAFE_SHARD}.json"

      - name: Upload summary artifact (consumed by #41 matrix publisher)
        uses: actions/upload-artifact@v4
        with:
          name: compat-suite-summary-${{ strategy.job-index }}
          path: knext/compat-suite-summary-*.json
          retention-days: 14
