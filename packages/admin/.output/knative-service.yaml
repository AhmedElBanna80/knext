# AUTO-GENERATED by kn-next build - DO NOT EDIT
# Source: kn-next.config.ts
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: kn-next-admin
  namespace: default
  labels:
    app: kn-next-admin
    generated-by: kn-next
spec:
  # Knative uses the latest ready revision by default
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/min-scale: "0"
        autoscaling.knative.dev/max-scale: "2"
    spec:
      serviceAccountName: kn-next-admin-sa
      containerConcurrency: 100
      timeoutSeconds: 300
      containers:
        - image: us-central1-docker.pkg.dev/gsw-mcp/knative-next-repo/kn-next-admin:1774076154288
          ports:
            - containerPort: 3000
          env:
            - name: HOSTNAME
              value: "0.0.0.0"
            - name: NODE_ENV
              value: "production"
            - name: CACHE_BUCKET_NAME
              value: "kn-next-admin-assets"
            - name: CACHE_BUCKET_REGION
              value: "us-east-1"
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 0
            periodSeconds: 1
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
---
# Least-privilege ServiceAccount to restrict K8s API access
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kn-next-admin-sa
  namespace: default
automountServiceAccountToken: false
